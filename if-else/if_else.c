 #include <stdio.h>
// int main(){
	 
	int x = -5;
	if(x<0){
		printf("%d is less than 0",x);
	} else {
		printf("%d is greater than 0",x);
	}
	

	return 0;
	 
 }//
 
  int main(){
	  int x=50;
	  int y=60;
	  if(x<y);{
		  printf("%d is less than %d",x,y);
	  } else {
		  printf("%d is greater than %d",x,y);
	  }
  }